import { useEffect } from 'react'

export const Errors = () => {
  useEffect(() => {
    // const socket = io('localhost:3001')
    // socket.on('diagnostics', (fileName, errors) => {
    //   console.log(errors)
    // })
    // return () => {
    //   socket.disconnect()
    // }
  }, [])
  return null
}
